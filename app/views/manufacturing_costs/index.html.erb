<h1>製品原価確認</h1>

<table id="asset-list-table">
<thead>
  <tr>
    <th>アイテムID</th>
    <th>アイテム名</th>
    <th>アイテム名(JA)</th>
    <th>製品原価(合計)</th>
    <th>製品原価(過去3回分)</th>
    <th>生産数合計</th>
    <th>金額合計(isk)</th>
  </tr>
</thead>

<tbody>
<% @costs.each do |cost| %>
  <tr>
    <td><%= cost.item_id %></td>
    <td><%= cost.item_name %></td>
    <td><%= cost.item_name_ja %></td>
    <td><%= cost.price %></td>
    <td><%= cost.price_of_top_n %></td>
    <td><%= cost.create_count %></td>
    <td><%= cost.sum_price %></td>
  </tr>
<% end %>
</tbody>
</table>

<br />

