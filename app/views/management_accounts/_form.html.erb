<%= form_for(@management_account) do |f| %>
  <% if @management_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@management_account.errors.count, "error") %> prohibited this management_account from being saved:</h2>

      <ul>
      <% @management_account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :purchase_amount %><br />
    <%= f.text_field :purchase_amount %>
  </div>
  <div class="field">
    <%= f.label :sales_amount %><br />
    <%= f.text_field :sales_amount %>
  </div>
  <div class="field">
    <%= f.label :inventory_amount %><br />
    <%= f.text_field :inventory_amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
