<%= form_for(@corp_location) do |f| %>
  <% if @corp_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@corp_location.errors.count, "error") %> prohibited this corp_location from being saved:</h2>

      <ul>
      <% @corp_location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :station_id %><br />
    <%= f.text_field :station_id %>
  </div>
  <div class="field">
    <%= f.label :station_name %><br />
    <%= f.text_field :station_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
